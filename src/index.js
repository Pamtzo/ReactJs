import React from "react";
import ReactDOM from "react-dom";

import Chat from "./views/chats/chats.view"

const rootElement = document.getElementById("root");
ReactDOM.render(<Chat />, rootElement);
